import dotenv from 'dotenv';
dotenv.config();


/* eslint-disable */
console.log(
    "Eva, can I see bees in a cave?".replace(/\W/gm, '')
    );

console.log(process.env.NEWCONST);
/* eslint-enable */
